{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd socialpulse-frontend && npm install && npm run build",
  "outputDirectory": "socialpulse-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
