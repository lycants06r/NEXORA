{
  "name": "nexora-command-center",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-stitch.mjs",
    "start": "npx serve public -l 8080",
    "dev": "npx serve public -l 8080"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.3.5"
  }
}
